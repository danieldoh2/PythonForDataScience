import turtle

import numpy as np
from numpy import arange
# importing a specific function from numpy.random file within numpy module
from numpy.random import randint
# this will make using functions more convenient since I won't have to do np.

# list = [[3.2,3], [3, 3, 1], [3,7,2]]
# np.array(list)
# print(list)
# j = (np.arange(0,11, 2)) #creates arr from start param to 2nd param with third param step size
# print(np.ones((5,2,3,4)))
# print(np.linspace(0,5,12))
# print(np.eye(4)) # must be one parameter, since it needs to a square (rows and columns must be equivalent)
# print(np.random.rand(1))
# print(np.random.randn(1)) #creates array with random numbers (rows, columns)
# j = (np.random.randint(1, 100, 10))
# print(j)
# print(arr.reshape(5,6)) #rows and columns must be filled up by correct number of elements
# print(arr.max()) #max val in array
# print(arr.min()) #min val in array
# print(j.argmax()) #returns index of max val
# print(j.argmin()) #returns index of min value
# print(j.shape) #calling the shape ATTRIBUTE of the object
